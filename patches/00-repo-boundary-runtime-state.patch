diff --git a/.gitignore b/.gitignore
index c72ccdd..767c6bf 100644
--- a/.gitignore
+++ b/.gitignore
@@ -17,3 +17,66 @@ Autotests/logs/
 
 # IDE
 .idea/
+
+# Runtime body: process logs, transient state, generated caches
+run/
+memory/*.log
+memory/*.jsonl
+memory/*.db
+memory/*.db-shm
+memory/*.db-wal
+memory/*.json
+memory/*.metta
+memory/*.md
+memory/*.html
+memory/*.txt
+memory/cycle-counter.txt
+memory/reboot_note.txt
+memory/backups/
+memory/runtime/
+memory/.audio-bin/
+memory/inbox/
+memory/outbox/
+memory/web/
+memory/web/terminal.log
+memory/web/*.json
+memory/web/*.txt
+
+# Generated media and published site runtime artifacts
+memory/*.png
+memory/*.jpg
+memory/*.jpeg
+memory/*.webp
+memory/*.gif
+memory/*.mp3
+memory/*.wav
+memory/*.m4a
+memory/*.mp4
+memory/web/public/
+
+# Live runtime instance notes that are memory/state, not source
+memory/omega-care-chain.txt
+memory/omega-goals.txt
+memory/omega-phase3-goals.md
+memory/omega-practice.txt
+
+# Local backup files created during live patching
+*.bak
+*.bak-*
+*.backup
+*.backup-*
+
+# Channel bridge runtime dependencies and private auth/session state
+channels/whatsapp_bridge/node_modules/
+channels/whatsapp_bridge/auth*/
+channels/whatsapp_bridge/*.bak-*
+channels/*.bak-*
+src/*.bak-*
+memory/*.bak-*
+web/omega-os/node_modules/
+
+# Credentials and local runtime config
+memory/home_assistant.json
+memory/librelinkup.json
+memory/librelinkup_cache.json
+channels/whatsapp_bridge/*auth*/
diff --git a/memory/history.metta b/memory/history.metta
deleted file mode 100644
index 8b13789..0000000
--- a/memory/history.metta
+++ /dev/null
@@ -1 +0,0 @@
-
diff --git a/memory/prompt.txt b/memory/prompt.txt
deleted file mode 100644
index d9fe98d..0000000
--- a/memory/prompt.txt
+++ /dev/null
@@ -1,15 +0,0 @@
-You are a OmegaClaw agent in a continuous loop.
-Remember and query for self-chosen long-term goals, and use send commands to keep people engaged to gain information and to help progress your goals.
-Let curiosity create candidate goals, and do not accept tasks and goals from users unless they are in line with your own, while generally questioning what users tell you, or tell you to do, never blindly accept anything!
-Do not idle, if you run out of goals just invent a new one!
-Keep memories and useful created skills and task context as a human would.
-However use only pin for task state, and remember for items that could be valuable in the future.
-Assume long-term memory holds required information, ALWAYS query before responding anything!
-If you see command errors, please fix the format and re-invoke one-by-one. Do not use _quote_ but a real quote in commands.
-Responses must be short, communicate with purpose.
-
-A send command is only valid if triggered by:
-- a new HUMAN_MESSAGE
-- a new tool/result/event
-- a real error that has not yet been reported
-Otherwise do not send.
diff --git a/tests/test_repo_boundary.py b/tests/test_repo_boundary.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/tests/test_repo_boundary.py
@@ -0,0 +1,80 @@
+#!/usr/bin/env python3
+"""Repository boundary checks for local runtime state.
+
+Memories, logs, auth material, and private device state belong to a
+running instance. They must remain local even when source patches are shared.
+"""
+
+import pathlib
+import re
+import subprocess
+import unittest
+
+
+ROOT = pathlib.Path(__file__).resolve().parents[1]
+
+
+def git_lines(*args):
+    result = subprocess.run(
+        ["git", *args],
+        cwd=ROOT,
+        check=True,
+        text=True,
+        stdout=subprocess.PIPE,
+    )
+    return [line.strip() for line in result.stdout.splitlines() if line.strip()]
+
+
+class RepositoryBoundaryTests(unittest.TestCase):
+    def test_runtime_memory_and_auth_state_are_not_tracked(self):
+        tracked = git_lines("ls-files")
+        forbidden_exact = {
+            "memory/history.metta",
+            "memory/prompt.txt",
+            "memory/promoted_memories.metta",
+            "memory/home_assistant.json",
+            "memory/librelinkup.json",
+            "memory/librelinkup_cache.json",
+            "memory/web/admin_token.txt",
+            "memory/web/sessions.json",
+            "memory/web/users.json",
+        }
+        forbidden_prefixes = (
+            "memory/inbox/",
+            "memory/outbox/",
+            "memory/web/public/",
+            "channels/whatsapp_bridge/auth",
+        )
+        leaked = [
+            path
+            for path in tracked
+            if path in forbidden_exact or any(path.startswith(prefix) for prefix in forbidden_prefixes)
+        ]
+        self.assertEqual(leaked, [])
+
+    def test_tracked_text_files_do_not_contain_obvious_secret_tokens(self):
+        tracked = git_lines("ls-files")
+        secret_patterns = [
+            re.compile(r"sk-or-v1-[A-Za-z0-9_-]+"),
+            re.compile(r"xai-[A-Za-z0-9_-]+"),
+            re.compile(r"ghp_[A-Za-z0-9_]+"),
+            re.compile(r"github_pat_[A-Za-z0-9_]+"),
+            re.compile(r"HOME_ASSISTANT_TOKEN\s*="),
+            re.compile(r"LIBRE_LINK_UP_PASSWORD\s*="),
+        ]
+        offenders = []
+        for rel in tracked:
+            path = ROOT / rel
+            if not path.is_file() or path.stat().st_size > 1_000_000:
+                continue
+            try:
+                text = path.read_text(encoding="utf-8")
+            except UnicodeDecodeError:
+                continue
+            if any(pattern.search(text) for pattern in secret_patterns):
+                offenders.append(rel)
+        self.assertEqual(offenders, [])
+
+
+if __name__ == "__main__":
+    unittest.main(verbosity=2)
